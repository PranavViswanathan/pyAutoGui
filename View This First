Before running the programs given here, make sure to run the following:

1.Run this on the admin terminal 

    pip install pywin32
    pip install keyboard
    pip install pyautogui
    pip install opencv-python

Without running this, the code will not run.
        
      
